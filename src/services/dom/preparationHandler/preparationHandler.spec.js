import PreparationHandler from "./preparationHandler";

describe("Preparation Handler unit test", () => {
	beforeEach(() => {});

	afterEach(() => {});

	describe("renderPage()", () => {
		test("should render html contents inside body", () => {});
		test("should add state to body class", () => {});
	});

	describe("DOM Events", () => {});
});
